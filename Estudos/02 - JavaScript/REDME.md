# JavaScript, ECMAScript, Node.js e npm

O **JavaScript** é uma linguagem de programação amplamente usada para o desenvolvimento web, trazendo interatividade e dinamismo às páginas. Ele segue o padrão **ECMAScript**, uma especificação que define as funcionalidades e a sintaxe da linguagem, garantindo a compatibilidade e evolução do JavaScript. 

Com o **Node.js**, o JavaScript pode ser executado no servidor, permitindo a criação de aplicações backend, como APIs e servidores, além de aplicações frontend. O Node.js é baseado no motor V8, que compila e executa o JavaScript de forma rápida.

O **npm** (Node Package Manager) é o gerenciador de pacotes do Node.js. Ele facilita a instalação, atualização e gerenciamento de bibliotecas e ferramentas JavaScript, permitindo que desenvolvedores compartilhem e usem código de terceiros. Isso cria um ecossistema completo, onde JavaScript e suas ferramentas estão disponíveis tanto para frontend quanto para backend, simplificando o desenvolvimento web.


## Terminal
Windows -> https://blog.formacao.dev/iniciando-com-o-terminal-do-windows/<br>
Linux/Mac -> https://blog.formacao.dev/terminal-no-macos-linux/

<sub>Mostrar versão do NODE.js</sub>
```
node --version
```
<sub>Mostrar versão do NPM</sub>
```
npm --version
```
